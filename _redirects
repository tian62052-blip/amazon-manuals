# Cloudflare Pages 跳转规则
# 语法: <来源路径>  <目标>  <状态码>
# 一律用 302（临时跳转）。301 会被手机浏览器永久缓存，
# 以后想改指向就改不动了 —— 这会让已印出的二维码报废。

# --- PDF 稳定别名 ---------------------------------------------
# 落地页和任何外部引用都只用 /b03/pdf 这个地址。
# 以后换 PDF 文件名，只改下面这一行，其它地方一律不动。
/b03/pdf            /B03-user-manual.pdf        302
/b03/manual.pdf     /B03-user-manual.pdf        302

# --- 大写路径兼容 ---------------------------------------------
# 二维码如果用全大写 URL 编码（更省模块数），需要这几条。
/B03                /b03/                       302
/B03/               /b03/                       302
/B03/PDF            /B03-user-manual.pdf        302

# --- 屏蔽内部文档 ---------------------------------------------
# 仓库里的文档和脚本会被 Pages 一并部署成可访问的静态文件。
# 二维码印在包装上，买家可能顺手翻目录，挡掉更干净。
/README.md          /                           404
/tools/*            /                           404
/archive/*          /                           404

# --- 未来新增型号，照抄上面的模式即可 --------------------------
# /b04/pdf          /B04-user-manual.pdf        302
